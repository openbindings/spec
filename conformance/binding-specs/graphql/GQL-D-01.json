{
  "rule": "GQL-D-01",
  "bindingSpec": "openbindings.graphql@1",
  "section": "4",
  "description": "Every source governed by this identifier carries a `location` that is an absolute URI whose scheme is `http` or `https`.",
  "notes": "§4: the scheme set is this specification's convention (the over-HTTP draft is the only incorporated single-result transport); a subscription endpoint rides `content.subscriptions`, never `location`.",
  "tests": [
    {
      "description": "https endpoint",
      "document": {
        "openbindings": "0.2.0",
        "operations": {
          "Viewer": {}
        },
        "sources": {
          "api": {
            "bindingSpec": "openbindings.graphql@1",
            "location": "https://api.example.test/graphql",
            "content": {
              "document": "query Viewer($id: ID!) { viewer(id: $id) { id name } }\nquery Status { status }\nquery Find($filter: Filter!) { find(filter: $filter) { id } }\nmutation Rename($id: ID!, $name: String!) { rename(id: $id, name: $name) { id name } }\n"
            }
          }
        },
        "bindings": {
          "Viewer.api": {
            "operation": "Viewer",
            "source": "api",
            "selector": "Viewer"
          }
        }
      },
      "valid": true
    },
    {
      "description": "http endpoint",
      "document": {
        "openbindings": "0.2.0",
        "operations": {
          "Viewer": {}
        },
        "sources": {
          "api": {
            "bindingSpec": "openbindings.graphql@1",
            "location": "http://api.example.test/graphql",
            "content": {
              "document": "query Viewer($id: ID!) { viewer(id: $id) { id name } }\nquery Status { status }\nquery Find($filter: Filter!) { find(filter: $filter) { id } }\nmutation Rename($id: ID!, $name: String!) { rename(id: $id, name: $name) { id name } }\n"
            }
          }
        },
        "bindings": {
          "Viewer.api": {
            "operation": "Viewer",
            "source": "api",
            "selector": "Viewer"
          }
        }
      },
      "valid": true
    },
    {
      "description": "location absent",
      "document": {
        "openbindings": "0.2.0",
        "operations": {
          "Viewer": {}
        },
        "sources": {
          "api": {
            "bindingSpec": "openbindings.graphql@1",
            "content": {
              "document": "query Viewer($id: ID!) { viewer(id: $id) { id name } }\nquery Status { status }\nquery Find($filter: Filter!) { find(filter: $filter) { id } }\nmutation Rename($id: ID!, $name: String!) { rename(id: $id, name: $name) { id name } }\n"
            }
          }
        },
        "bindings": {
          "Viewer.api": {
            "operation": "Viewer",
            "source": "api",
            "selector": "Viewer"
          }
        }
      },
      "valid": false,
      "violates": [
        "GQL-D-01"
      ]
    },
    {
      "description": "relative location",
      "document": {
        "openbindings": "0.2.0",
        "operations": {
          "Viewer": {}
        },
        "sources": {
          "api": {
            "bindingSpec": "openbindings.graphql@1",
            "location": "/graphql",
            "content": {
              "document": "query Viewer($id: ID!) { viewer(id: $id) { id name } }\nquery Status { status }\nquery Find($filter: Filter!) { find(filter: $filter) { id } }\nmutation Rename($id: ID!, $name: String!) { rename(id: $id, name: $name) { id name } }\n"
            }
          }
        },
        "bindings": {
          "Viewer.api": {
            "operation": "Viewer",
            "source": "api",
            "selector": "Viewer"
          }
        }
      },
      "valid": false,
      "violates": [
        "GQL-D-01",
        "OBI-D-05"
      ]
    },
    {
      "description": "WebSocket scheme is not an incorporated transport for the endpoint",
      "document": {
        "openbindings": "0.2.0",
        "operations": {
          "Viewer": {}
        },
        "sources": {
          "api": {
            "bindingSpec": "openbindings.graphql@1",
            "location": "wss://api.example.test/graphql",
            "content": {
              "document": "query Viewer($id: ID!) { viewer(id: $id) { id name } }\nquery Status { status }\nquery Find($filter: Filter!) { find(filter: $filter) { id } }\nmutation Rename($id: ID!, $name: String!) { rename(id: $id, name: $name) { id name } }\n"
            }
          }
        },
        "bindings": {
          "Viewer.api": {
            "operation": "Viewer",
            "source": "api",
            "selector": "Viewer"
          }
        }
      },
      "valid": false,
      "violates": [
        "GQL-D-01"
      ]
    },
    {
      "description": "location is not a string",
      "document": {
        "openbindings": "0.2.0",
        "operations": {
          "Viewer": {}
        },
        "sources": {
          "api": {
            "bindingSpec": "openbindings.graphql@1",
            "location": {
              "href": "https://api.example.test/graphql"
            },
            "content": {
              "document": "query Viewer($id: ID!) { viewer(id: $id) { id name } }\nquery Status { status }\nquery Find($filter: Filter!) { find(filter: $filter) { id } }\nmutation Rename($id: ID!, $name: String!) { rename(id: $id, name: $name) { id name } }\n"
            }
          }
        },
        "bindings": {
          "Viewer.api": {
            "operation": "Viewer",
            "source": "api",
            "selector": "Viewer"
          }
        }
      },
      "valid": false,
      "violates": [
        "GQL-D-01"
      ]
    }
  ]
}