edition = "2026";
package enumdemo;

import "google/protobuf/json_enumvalue_options.proto";

enum State {
  STATE_UNSPECIFIED = 0;
  STATE_READY = 1 [(pb.enumvalue.json).string = "ready-value"];
}

message EnumValue {
  State enum_state = 1 [json_name = "state"];
}
